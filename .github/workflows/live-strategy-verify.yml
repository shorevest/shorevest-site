name: One-time live Strategy verification

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/live-strategy-verify.yml"

permissions:
  contents: write

jobs:
  verify:
    if: github.event.head_commit.message == 'Add final live Strategy verification'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify live Strategy CSS and image
        shell: bash
        run: |
          set -u
          sleep 20
          ts=$(date +%s)
          page_code=$(curl -L -sS -o /tmp/strategy.html -w '%{http_code}' -H 'Cache-Control: no-cache' -H 'Pragma: no-cache' "https://shorevest.com/strategy/?_verify=${ts}")
          css_code=$(curl -L -sS -o /tmp/strategy.css -w '%{http_code}' -H 'Cache-Control: no-cache' -H 'Pragma: no-cache' "https://shorevest.com/assets/css/visual-review-overrides.css?_verify=${ts}")
          image_code=$(curl -L -sS -o /dev/null -w '%{http_code}' -H 'Cache-Control: no-cache' -H 'Pragma: no-cache' "https://shorevest.com/yang-miao-YUJNtZS8n5o-unsplash.jpg?_verify=${ts}")
          css_yang=no; page_override=no
          grep -Fq 'yang-miao-YUJNtZS8n5o-unsplash.jpg' /tmp/strategy.css && css_yang=yes || true
          grep -Fq 'visual-review-overrides.css' /tmp/strategy.html && page_override=yes || true
          printf 'checked_at=%s\npage_http=%s\ncss_http=%s\nimage_http=%s\ncss_yang=%s\npage_override=%s\n' "$(date -u +%Y-%m-%dT%H:%M:%SZ)" "$page_code" "$css_code" "$image_code" "$css_yang" "$page_override" > live-strategy-verify.txt
          cat live-strategy-verify.txt
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add live-strategy-verify.txt
          git commit -m 'Record final live Strategy verification' || exit 0
          git push
